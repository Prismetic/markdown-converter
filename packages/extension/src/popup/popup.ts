import type { ExtMsg } from '../shared/messages.js';

// Popup entry: handles non-PDF conversions in DOM context
// Receives convert messages from content scripts via chrome.runtime

// Minimal stub — implementation added in M2
void (0 as unknown as ExtMsg);
